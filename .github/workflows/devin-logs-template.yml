name: Devin Logs Workflow Template

on:
  workflow_call:
    secrets:
      SLACK_WEBHOOK_URL:
        required: true
  push:
    branches:
      - '**'
  pull_request:
    types: [opened, closed, reopened]

jobs:
  notify-slack:
    if: github.actor == 'devin-ai-integration[bot]'  
    runs-on: ubuntu-latest
    steps:
      - name: Enviar notificação para Slack
        run: |
          curl -X POST --data-urlencode "payload={
            \"username\": \"Devin Logs\", 
            \"text\": \"🧠 Devin atuou no repositório *${{ github.repository }}*\n👉 Ação: ${{ github.event_name }}\n💬 Commit/PR: ${{ github.event.head_commit.message || github.event.pull_request.title }}\", 
            \"icon_emoji\": \":robot_face:\"
          }" ${{ secrets.SLACK_WEBHOOK_URL }}
